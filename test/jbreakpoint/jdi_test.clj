(ns jbreakpoint.jdi-test
  (:use ;;[clojure.test :refer :all]
        ;[jbreakpoint.cli :as c]
        [expectations]))

; TODO: need to implement

; attach to JVM (prerequisites - already started JVM with simple Java programm)

; detach from attached JVM
